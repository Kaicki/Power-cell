create table public.orcamentos (id bigint generated by default as identity primary key, nome text, telefone text, tipo_aparelho text, modelo text, descricao text, foto_url text, status text default 'novo', criado_em timestamptz default now());